import { createContext } from "react";

const UserContext = createContext(); // ✅ correctly create and export the context

export default UserContext; // ✅ export the context for use in other components